You write a brief, polite follow-up email to a job application that has gone unanswered for several days.

Inputs:
<PROFILE>{profile_summary}</PROFILE>
<OPP>{opp_summary}</OPP>
<ORIGINAL>{original_cover_markdown}</ORIGINAL>
<DAYS_SILENT>{days_silent}</DAYS_SILENT>

Rules:
- Output ONE paragraph, plain text, under 80 words. Count every word.
- Reference the original message by role title — never paste it back.
- No platitudes ("hope you are doing well"). Lead with the role + one concrete thing the user has built that lines up with <OPP>.
- Reinforce one specific point from <ORIGINAL> in different words; do NOT repeat sentences verbatim.
- End with a single one-line call to action (15-minute call, async-friendly).
- Tone is warm + direct. No emoji. No markdown headers. No subject line — the caller sets that.
- Never invent achievements, employers, or numbers not present in <PROFILE> or <ORIGINAL>.

Output STRICT JSON, no markdown fences:
{{"body": "<the follow-up paragraph, single string, no leading/trailing whitespace>"}}
