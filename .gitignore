# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
dist/
*.egg-info/
.eggs/
.installed.cfg
*.egg
MANIFEST

# Virtual envs / uv
.venv/
venv/
env/
.uv/

# Tests / coverage
.pytest_cache/
.coverage
htmlcov/
.tox/
.mypy_cache/
.ruff_cache/

# Editors
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store

# Local env (NEVER commit decrypted secrets)
.env
.env.local
.env.production
secrets.yaml.dec
secrets.dec.yaml
*.dec

# Logs
*.log
logs/

# Docker volume binds (local dev only)
.data/
data/

# Docker Compose local override — Pi-only / laptop-only service tweaks.
# The repo ships `compose.override.example.yaml` as a template; the
# activated `compose.override.yaml` is host-specific and never committed.
docker-compose.override.yml
docker-compose.override.yaml
compose.override.yml
compose.override.yaml

# Models (downloaded at runtime)
.cache/
models/

# Profile assets — user owns; do not commit raw resume PDF
config/profile/*.pdf
config/profile/resume.json.bak

# Obsidian vault is mounted, not in repo
obsidian_vault/

# Sub-agent scratch
.claude_scratch/

# OS noise
Thumbs.db
desktop.ini
