# Bootstrap-only. After secrets.yaml is SOPS-encrypted, this file should be empty.
# Compose runs via:   sops exec-env secrets.yaml 'docker compose up -d'
# Do NOT commit a populated .env.

# Used pre-SOPS for local smoke tests only:
POSTGRES_USER=marked
POSTGRES_PASSWORD=changeme
POSTGRES_DB=marked
REDIS_PASSWORD=changeme

# Path to SOPS-encrypted secrets file (Pi-deployed)
SOPS_AGE_KEY_FILE=/home/lakshit_gupta/.config/sops/age/keys.txt
